<%= form_for(@scriptconfig) do |f| %>
  <% if @scriptconfig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scriptconfig.errors.count, "error") %> prohibited this scriptconfig from being saved:</h2>

      <ul>
      <% @scriptconfig.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :script_params %><br>
    <%= f.text_area :script_params %>
  </div>
  <div class="field">
    <%= f.label :config_id %><br>
    <%= f.number_field :config_id %>
  </div>
  <div class="field">
    <%= f.label :script_id %><br>
    <%= f.number_field :script_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
